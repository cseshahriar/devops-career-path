Docker class 3
--------------
docker pull nginx:stable-alpine
docker run --name amader-nginx-alpine:v1 -d nginx:stable-alpine
docker build -t amader-nginx-alpine:v1 .
docker run --rm -it amader-nginx-alpine:v2 bash

# build and rut
docker build -t amader-nginx-alpine:v3 --build-arg AIOMSC_VERSION=17.5.31 .
docker run --rm -it amader-nginx-alpine:v3 bash

docker images -f "dangling=true" -q | xargs docker rm -f

export FLASK_SECRET_KEY="ddfd3434dfd343"
env | wc -l

docker build -t amader-nginx-alpine:v5  bash


docker run --rm -it --env-file .env amader-nginx-alpine:v5 sh

docker ps -a
docker start id
docker build -t amader-nginx-alpine:v6 .
docker inspect amader-nginx-alpine:v6
docker run --rm amader-nginx-alpine:v6

# build api dir ar sapekke
docker build -t amader-nginx-alpine:v9 -f Dockerfile api/


------------------------
class 2
--------------------------
# image create
# container create from image
# run the container
docker build -t mycoolwebapp:v1
docker build -t mycoolwebapp:v1 --no-cache

# bind for sync with local machine code# Created by https://www.toptal.com/developers/gitignore/api/python
# Edit at https://www.toptal.com/developers/gitignore?templates=python

### Python ###
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/#use-with-ide
.pdm.toml

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

### Python Patch ###
# Poetry local configuration file - https://python-poetry.org/docs/configuration/#local-configuration
poetry.toml

# ruff
.ruff_cache/

# LSP config files
pyrightconfig.json

# End of https://www.toptal.com/developers/gitignore/api/python
docker volume ls | grep mycoolwebapp
docker exec -it id bash
ls

- docker image rebuild => container recreated
                                    (prev version)

1.13"

bind for code
volume: database data, media files
data: /var/lib/mysql

------
volume
------
volume=drive, database data, file upload
volume for data precept
volume use for not data lose when container will delete

-------------------
docker network type
-------------------
1. Bridge network (inter container communication) default
2. Host (port did not buinding)
3. None (Isolation, no connection)
4. macvlan(like physical device)
5. ipvlan(ip configuration)
6. overlay(swarm, ak ba akadin node communication)


docker network ls

container crate:
docker run --name container-1 -d --network amader-network nginx
docker run --name container-2 -d --network amader-network nginx

docker inspect id --format='{{.State.Status}}'
docker inspect id --format='{{.NetworkSettings.Networks}}'

docker exec -it container-1 bash
host container-2
https://ioflood.com/blog/install-host-command-linux/

docker run --name container-3 -d --network host mycoolwebapp:v5
https://www.portainer.io/